import { SlicesColumnText } from "~/types/generated/components";

export default function ColumnText({ props }: SlicesColumnText) {
  return (
    <div className="container my-10 flex items-center justify-center">
      <span>ColumnText</span>
    </div>
  );
}
