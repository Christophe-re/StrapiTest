import { SlicesFooter } from "~/types/generated/components";

export default function Footer({ props }: SlicesFooter) {
  return (
    <div className="container my-10 flex items-center justify-center">
      <span>Footer</span>
    </div>
  );
}
