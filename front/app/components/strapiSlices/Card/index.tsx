import { SlicesCard } from "~/types/generated/components";

export default function Card({ props }: SlicesCard) {
  return (
    <div className="container my-10 flex items-center justify-center">
      <span>CARD</span>
    </div>
  );
}
